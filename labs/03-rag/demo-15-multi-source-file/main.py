"""
Main entry point for the multi-source document loading practice.

This module serves as the entry point when running the project with:
    uv run main.py
"""

from main.multi_source_loader import main

if __name__ == "__main__":
    main()

